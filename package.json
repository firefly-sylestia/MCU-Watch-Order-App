{
  "name": "mcu-viewing-order",
  "version": "2.0.0",
  "type": "module",
  "scripts": {
    "web:dev": "npm --prefix webApp run dev",
    "web:build": "npm --prefix webApp run build",
    "web:preview": "npm --prefix webApp run preview",
    "android:build:debug": "gradle :androidApp:assembleDebug",
    "android:build:release": "gradle :androidApp:assembleRelease",
    "data:validate": "node tools/validate-shared-data.mjs",
    "data:generate": "node tools/generate-kotlin-data.mjs && node tools/generate-web-data.mjs",
    "check:theme-colors": "node webApp/scripts/check-theme-colors.mjs"
  },
  "dependencies": {},
  "devDependencies": {}
}
