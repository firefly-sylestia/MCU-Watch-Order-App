import fs from 'node:fs';
import path from 'node:path';
const read = (name) => JSON.parse(fs.readFileSync(path.join('shared-data', name), 'utf8'));
const catalog = { titles: [...read('mcu-titles.json'), ...read('dc-titles.json')], timelineModes: read('timeline-modes.json'), trailers: read('trailers.json'), afterCredits: read('after-credits.json'), collections: read('collections.json'), phases: read('phases.json') };
fs.mkdirSync('webApp/src/data', { recursive: true });
fs.writeFileSync('webApp/src/data/generatedCatalog.js', `// Generated by tools/generate-web-data.mjs. Do not edit by hand.\nexport const GENERATED_CATALOG = ${JSON.stringify(catalog, null, 2)};\nexport const GENERATED_TITLES = GENERATED_CATALOG.titles;\n`);
console.log(`Generated webApp/src/data/generatedCatalog.js with ${catalog.titles.length} titles.`);
